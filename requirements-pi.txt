# ==========================================
# A-PAS 프로젝트 - Raspberry Pi 5 배포 환경
# Hailo-8 AI HAT+ (26 TOPS) + ONNX Runtime
# ==========================================
# 설치: pip install -r requirements-pi.txt

# --- AI 추론 엔진 ---
onnxruntime               # LSTM 궤적 예측 모델 추론 (CPU)

# --- 영상 처리 ---
opencv-python-headless    # 카메라/HDMI 캡처 영상 처리 (GUI 미포함 경량 버전)
Pillow                    # 이미지 전처리 (리사이즈, 크롭)

# --- YOLOv8 (Hailo-8 미사용 시 fallback) ---
ultralytics               # YOLOv8 ByteTrack 추론 + 추적

# --- 하드웨어 제어 (GPIO / LED / 부저) ---
rpi-lgpio                 # RPi 5 전용 GPIO (부저 제어)
rpi_ws281x                # WS2812B LED Strip 제어

# --- 데이터 연산 ---
numpy                     # 배열/행렬 연산

# --- 시스템 모니터링 ---
psutil                    # CPU 온도, 메모리 사용량 체크

# --- 통신 (선택) ---
# requests                # 관제실 API 전송 시
# pyserial                # 아두이노 시리얼 통신 시

# ==========================================
# 참고: Hailo-8 SDK (hailort, hailo-platform)는
# Hailo 공식 apt 저장소에서 별도 설치
# sudo apt install hailort hailo-tappas-core
# ==========================================